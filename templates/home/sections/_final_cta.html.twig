<section class="py-20">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl font-display font-bold tracking-tight text-ink sm:text-4xl">Stop finding out about problems from your customers</h2>
        <p class="mt-4 text-lg font-body text-ink-muted">Set up your first monitor in under five minutes.</p>
        <div class="mt-8">
            {% import 'components/_macros.html.twig' as m %}
            {{ m.button('Get started for free', path('app_login')) }}
        </div>
        {# TODO: confirm actual trial terms #}
        <p class="mt-4 font-mono text-xs text-ink-muted">14-day free trial · no card required</p>
    </div>
</section>
